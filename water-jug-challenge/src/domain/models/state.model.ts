export class State {
    public bucketX : number;
    public bucketY: number;

    constructor(bucketX: number, bucketY: number) {
        this.bucketX = bucketX;
        this.bucketY = bucketY;
    }
}